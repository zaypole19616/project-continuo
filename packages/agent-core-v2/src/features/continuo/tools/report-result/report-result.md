Report the outcome of the current task so the product can verify it. Call this once, right before your final answer, whenever the task produced or changed files.

Give the exact paths of the files the user should look at (relative to the workspace root), one short note per file, a short summary, and anything you could not finish or verify under `unresolved`. The product checks that every reported path exists; a missing path or an unresolved item sends the task to review instead of marking it complete.
