---
"@moonshot-ai/kimi-code": minor
---

In the experimental Continuo mode (`KIMI_CODE_EXPERIMENTAL_CONTINUO=1`), each branched line of work now gets its own copy of the project folder, so a new plan never overwrites another line's files.
