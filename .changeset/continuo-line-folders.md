---
"@moonshot-ai/kimi-code": minor
---

In the experimental Continuo mode (`KIMI_CODE_EXPERIMENTAL_CONTINUO=1`), a branch of a task no longer overwrites files from other branches; it saves its own version next to them instead.
