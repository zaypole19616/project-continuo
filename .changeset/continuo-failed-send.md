---
"@moonshot-ai/kimi-code": patch
---

In the experimental Continuo mode (`KIMI_CODE_EXPERIMENTAL_CONTINUO=1`), a message that fails to send stays in the input box, and while a question or approval is open the input box asks you to handle it first.
