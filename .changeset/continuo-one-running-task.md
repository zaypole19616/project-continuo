---
"@moonshot-ai/kimi-code": patch
---

In the experimental Continuo mode (`KIMI_CODE_EXPERIMENTAL_CONTINUO=1`), continuing or replying to a task is refused while another task on the same line is still running, so neither gets stuck.
